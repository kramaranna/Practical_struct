#include <iostream>
#include <string>
using namespace std;

struct Book {
    string title;
    int pages;
    float price;
    // 👈 студенти можуть додати поле author
};

int main() {
    Book library[2]; // 👈 студенти заповнюють книги

    // for (...) { ... } — дописати цикл

    return 0;
}
