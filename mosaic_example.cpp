#include <iostream>
#include <string>
using namespace std;

struct Book {
    string title;
    int pages;
    float price;
    // 👈  додати поле author
};

int main() {
    Book library[2]; // 👈  заповнити книги

    // for (...) { ... } — дописати цикл

    return 0;
}
