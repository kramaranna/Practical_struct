
name: Check Car
on: [push]
jobs:
  check_car:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check Car struct
        run: |
          grep -q "struct Car" cpp_examples/car_example.cpp
          echo "✅ Car struct found"
