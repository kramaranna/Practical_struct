name: ❓ Хто я? (Car)

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  check_car:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🚗 Перевірка структури Car
        run: |
          grep -q "struct Car" *.cpp || { echo "❌ Немає структури Car"; exit 1; }
          grep -q "string brand" *.cpp || { echo "⚠️ Немає поля brand"; exit 1; }
          grep -q "int year" *.cpp || { echo "⚠️ Немає поля year"; exit 1; }
          grep -q "int speed" *.cpp || { echo "⚠️ Немає поля speed"; exit 1; }
          echo "✅ Ви правильно описали структуру Car!"
