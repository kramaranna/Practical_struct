name: 🏠 Перевірка циклу пошуку

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  check_loop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🔎 Перевірка циклу for і умови if
        run: |
          if grep -q "for" *.cpp && grep -q "if" *.cpp; then
            echo "✅ Знайдено цикл і перевірку умови!"
          else
            echo "❌ Не знайдено цикл або умову. Спробуйте ще раз."
            exit 1
          fi

      - name: 🏁 Результат
        run: echo "🎯 Ви успішно виконали пошук у масиві структур!"
