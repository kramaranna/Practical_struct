name: 🧩 Компіляція програми

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🛠️ Компіляція
        run: |
          g++ *.cpp -o program
          echo "✅ Програму успішно скомпільовано!"

      - name: 🚀 Запуск програми
        run: |
          ./program || echo "⚠️ Програму запущено, але перевірте вивід."
