name: Check Me
on: [push]
jobs:
  check_me:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check Me struct
        run: |
          grep -q "struct Me" cpp_examples/me_example.cpp
          echo "✅ Me struct found"
