#include <iostream>
#include <string>
using namespace std;

struct Car {
    string brand;
    int year;
    int speed;
    // 👈 Можна додати поле color
};

int main() {
    Car cars[2]; // 👈  заповнити масив

    // for (...) { ... } — дописати цикл

    return 0;
}
